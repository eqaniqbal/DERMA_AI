name: derma_ai
description: Derma AI - Skin Care App
publish_to: 'none'

environment:
  sdk: '^3.11.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.0
  provider: ^6.1.1
  flutter_secure_storage: ^9.0.0
  flutter_test:
    sdk: flutter
  image_picker: ^1.0.4

flutter:
  uses-material-design: true